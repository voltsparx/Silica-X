x-sylica-common: &sylica-common
  stdin_open: true
  tty: true
  init: true
  read_only: true
  tmpfs:
    - /tmp
    - /home/sylica
  cap_drop:
    - ALL
  security_opt:
    - no-new-privileges:true
  pids_limit: ${SYLICA_DOCKER_PIDS_LIMIT:-256}
  mem_limit: ${SYLICA_DOCKER_MEM_LIMIT:-1g}
  stop_grace_period: 15s
  volumes:
    - ../output:/app/output
  environment:
    PYTHONUNBUFFERED: "1"
    SYLICA_RUNTIME: docker
  healthcheck:
    test: ["CMD", "python", "-c", "import core; import core.runner; print('ok')"]
    interval: 30s
    timeout: 5s
    retries: 3
    start_period: 10s

services:
  silica-x:
    <<: *sylica-common
    build:
      context: ..
      dockerfile: docker/Dockerfile
      args:
        PYTHON_VERSION: ${SYLICA_PYTHON_VERSION:-3.13}
    container_name: silica-x

  silica-x-tor:
    <<: *sylica-common
    profiles: ["tor"]
    build:
      context: ..
      dockerfile: docker/Dockerfile.tor
      args:
        PYTHON_VERSION: ${SYLICA_PYTHON_VERSION:-3.13}
    container_name: silica-x-tor
